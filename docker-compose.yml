version: '3.8'

services:
  ai-service:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      # GROQ_API_KEY should be passed from the environment or .env file
    env_file:
      - .env
